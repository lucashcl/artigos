export type Summary = {
   id: string
   url: string
   title: string
   summary: string
   tags: string
   created_at: string
}

export type QueueItem = {
   id: string
   url: string
   status: string
   created_at: string
}
